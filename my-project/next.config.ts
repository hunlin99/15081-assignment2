import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export', // Enables static export
  basePath: '/15081-assignment2',
  assetPrefix: '/15081-assignment2',
};

export default nextConfig;
